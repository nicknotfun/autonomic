[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "autonomic-sdk"
version = "0.1.0"
description = "Python SDK for Autonomic Mirage Media Server, MRAD/MAS, and direct amplifier control APIs"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Autonomic SDK contributors"}]
keywords = ["autonomic", "mirage", "mrad", "mms", "media-server", "amplifier"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Home Automation",
]
dependencies = ["pydantic>=2.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
include = ["autonomic", "autonomic.*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
